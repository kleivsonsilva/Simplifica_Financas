services:

  - type: web
    name: simplifica-auth
    runtime: python
    region: oregon
    plan: free
    buildCommand: pip install -r auth-service/requirements.txt
    startCommand: cd auth-service && gunicorn --bind 0.0.0.0:10000 --workers 2 --timeout 120 app:app
    healthCheckPath: /health
    envVars:
      - key: SERVICE_PORT
        value: "10000"
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: JWT_SECRET
        sync: false

  - type: web
    name: simplifica-transactions
    runtime: python
    region: oregon
    plan: free
    buildCommand: pip install -r transaction-service/requirements.txt
    startCommand: cd transaction-service && gunicorn --bind 0.0.0.0:10000 --workers 2 --timeout 120 app:app
    healthCheckPath: /health
    envVars:
      - key: SERVICE_PORT
        value: "10000"
      - key: MONGO_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: RABBITMQ_URL
        sync: false
      - key: JWT_SECRET
        sync: false

  - type: web
    name: simplifica-goals
    runtime: python
    region: oregon
    plan: free
    buildCommand: pip install -r goal-service/requirements.txt
    startCommand: cd goal-service && gunicorn --bind 0.0.0.0:10000 --workers 2 --timeout 120 app:app
    healthCheckPath: /health
    envVars:
      - key: SERVICE_PORT
        value: "10000"
      - key: MONGO_URL
        sync: false
      - key: RABBITMQ_URL
        sync: false
      - key: JWT_SECRET
        sync: false

  - type: web
    name: simplifica-reports
    runtime: python
    region: oregon
    plan: free
    buildCommand: pip install -r report-service/requirements.txt
    startCommand: cd report-service && gunicorn --bind 0.0.0.0:10000 --workers 2 --timeout 120 app:app
    healthCheckPath: /health
    envVars:
      - key: SERVICE_PORT
        value: "10000"
      - key: MONGO_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: JWT_SECRET
        sync: false

  - type: web
    name: simplifica-notifications
    runtime: python
    region: oregon
    plan: free
    buildCommand: pip install -r notification-service/requirements.txt
    startCommand: cd notification-service && gunicorn --bind 0.0.0.0:10000 --workers 2 --timeout 120 app:app
    healthCheckPath: /health
    envVars:
      - key: SERVICE_PORT
        value: "10000"
      - key: REDIS_URL
        sync: false
      - key: RABBITMQ_URL
        sync: false
      - key: JWT_SECRET
        sync: false

  - type: web
    name: simplifica-gateway
    runtime: python
    region: oregon
    plan: free
    buildCommand: pip install -r api-gateway/requirements.txt
    startCommand: cd api-gateway && gunicorn --bind 0.0.0.0:10000 --workers 2 --timeout 120 app:app
    healthCheckPath: /health
    envVars:
      - key: GATEWAY_PORT
        value: "10000"
      - key: AUTH_SERVICE_URL
        value: https://simplifica-auth.onrender.com
      - key: TRANSACTION_SERVICE_URL
        value: https://simplifica-transactions.onrender.com
      - key: GOAL_SERVICE_URL
        value: https://simplifica-goals.onrender.com
      - key: REPORT_SERVICE_URL
        value: https://simplifica-reports.onrender.com
      - key: NOTIFICATION_SERVICE_URL
        value: https://simplifica-notifications.onrender.com
      - key: REDIS_URL
        sync: false
      - key: JWT_SECRET
        sync: false

  - type: web
    name: simplifica-frontend
    runtime: python
    region: oregon
    plan: free
    buildCommand: pip install -r frontend/requirements.txt
    startCommand: cd frontend && gunicorn --bind 0.0.0.0:10000 --workers 2 --timeout 120 app:app
    healthCheckPath: /health
    envVars:
      - key: FRONTEND_PORT
        value: "10000"
      - key: API_GATEWAY_URL
        value: https://simplifica-gateway.onrender.com
      - key: JWT_SECRET
        sync: false
